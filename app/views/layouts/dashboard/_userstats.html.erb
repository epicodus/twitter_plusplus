<p class="lead">
  <%= image_tag user.avatar, class: 'avatar-image' %>
  @<%= user.handle %>
</p>
<table class="table">
  <thead>
    <th>Tweets</th>
    <th>Following</th>
    <th>Followers</th>
  </thead>
  <tr>
    <td><%= user.tweets.length %></td>
    <td><%= user.followed_users.length %></td>
    <td><%= user.followers.length %></td>
  </tr>
</table>
